{
  "name": "cad-viewer-desktop",
  "private": true,
  "version": "0.0.0",
  "description": "CAD Viewer desktop shell (Tauri). Runs the bundled Python CAD backend sidecar.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:runtime": "../scripts/desktop/build-python-runtime.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
