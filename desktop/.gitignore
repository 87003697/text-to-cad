# Tauri / Rust build output
src-tauri/target/
src-tauri/gen/

# Generated, per-platform Python runtime + sidecar binary (built by
# scripts/desktop/build-python-runtime.sh; never committed — large + platform-specific)
src-tauri/python/
src-tauri/binaries/

# App icons are generated from a source logo via `npm run tauri icon <logo>`
# (one-time setup, see README) — provide a real logo; the placeholders aren't committed.
src-tauri/icons/

# Node
node_modules/
